knitr::opts_chunk$set(echo = TRUE)
library(knitr)
Farbe <- c("hellweiß", "schwarzmetallic", "limette", "orange", "rot")
Nennung <- c(130, 104, 325, 455, 286)
huellen_farben <- data.frame(Farbe, Nennung)
kable(huellen_farben)
?barplot()
library(knitr)
Farbe <- c("hellweiß", "schwarzmetallic", "limette", "orange", "rot")
Nennung <- c(130, 104, 325, 455, 286)
huellen_farben <- data.frame(Farbe, Nennung)
kable(huellen_farben)
barplot(height=huellen_farben$nennung, names=huellen_farben$Farbe)
barplot(huellen_farben$Nennung, names.arg = huellen_farben$Farbe)
barplot(huellen_farben$Nennung, names.arg = huellen_farben$Farbe,
col = rainbow(nrow(huellen_farben)),
main = "Anzahl der Nennungen nach Farbe",
xlab = "Farbe",
ylab = "Anzahl der Nennungen")
barplot(huellen_farben$Nennung, names = huellen_farben$Farbe,
col = rainbow(nrow(huellen_farben)),
main = "Anzahl der Nennungen nach Farbe",
xlab = "Farbe",
ylab = "Anzahl der Nennungen")
barplot(huellen_farben$Nennung, names = huellen_farben$Farbe,
col = rainbow(nrow(huellen_farben)),
main = "Anzahl der Nennungen nach Farbe",
xlab = "Farbe",
ylab = "Anzahl der Nennungen")
barplot(huellen_farben$Nennung, names.arg = huellen_farben$Farbe,
col = rainbow(nrow(huellen_farben)),
main = "Anzahl der Nennungen nach Farbe",
xlab = "Farbe",
ylab = "Anzahl der Nennungen")
barplot(huellen_farben$Nennung, names.arg = huellen_farben$Farbe)
barplot(huellen_farben$Nennung, names = huellen_farben$Farbe,
col = rainbow(nrow(huellen_farben)),
main = "Anzahl der Nennungen nach Farbe",
xlab = "Farbe",
ylab = "Anzahl der Nennungen")
barplot(height = huellen_farben$Nennung, names = huellen_farben$Farbe,
col = rainbow(nrow(huellen_farben)),
main = "Anzahl der Nennungen nach Farbe",
xlab = "Farbe",
ylab = "Anzahl der Nennungen")
barplot(height = huellen_farben$Nennung, names = huellen_farben$Farbe,
col = rainbow(nrow(huellen_farben)),
main = "Anzahl der Nennungen nach Farbe",
xlab = "Farbe",
ylab = "Anzahl der Nennungen")
barplot(height = huellen_farben$Nennung, names = huellen_farben$Farbe,
col = farben_colors,
main = "Anzahl der Nennungen nach Farbe",
xlab = "Farbe",
ylab = "Anzahl der Nennungen")
farben_colors <- c("white", "black", "lime", "orange", "red")
barplot(height = huellen_farben$Nennung, names = huellen_farben$Farbe,
col = farben_colors,
main = "Anzahl der Nennungen nach Farbe",
xlab = "Farbe",
ylab = "Anzahl der Nennungen")
farben_colors <- c("white", "black", "lime", "orange", "red")
barplot(height = huellen_farben$Nennung, names = huellen_farben$Farbe,
col = farben_colors,
main = "Anzahl der Nennungen nach Farbe",
xlab = "Farbe",
ylab = "Anzahl der Nennungen")
farben_colors <- c("white", "black", "green", "orange", "red")
barplot(height = huellen_farben$Nennung, names = huellen_farben$Farbe,
col = farben_colors,
main = "Anzahl der Nennungen nach Farbe",
xlab = "Farbe",
ylab = "Anzahl der Nennungen")
# Specify the colors corresponding to each category
farben_colors <- c("white", "black", "green", "orange", "red")
# Create the barplot using the specified colors
barplot(height = huellen_farben$Nennung, names = huellen_farben$Farbe,
col = farben_colors,
main = "Anzahl der Nennungen nach Farbe",
xlab = "Farbe",
ylab = "Anzahl der Nennungen")
# Add text labels inside or above the bars
text(bp, huellen_farben$Nennung, labels = huellen_farben$Nennung, pos = 3, cex = 0.8)
# Specify the colors corresponding to each category
farben_colors <- c("white", "black", "green", "orange", "red")
# Create the barplot using the specified colors
bp <- barplot(height = huellen_farben$Nennung, names = huellen_farben$Farbe,
col = farben_colors,
main = "Anzahl der Nennungen nach Farbe",
xlab = "Farbe",
ylab = "Anzahl der Nennungen")
# Add text labels inside or above the bars
text(bp, huellen_farben$Nennung, labels = huellen_farben$Nennung, pos = 3, cex = 0.8)
# Add text labels inside or above the bars
text(bp, huellen_farben$Nennung, labels = huellen_farben$Nennung, pos = 3, cex = 0.8)
# Specify the colors corresponding to each category
farben_colors <- c("white", "black", "green", "orange", "red")
# Create the barplot using the specified colors
bp <- barplot(height = huellen_farben$Nennung, names = huellen_farben$Farbe,
col = farben_colors,
main = "Anzahl der Nennungen nach Farbe",
xlab = "Farbe",
ylab = "Anzahl der Nennungen")
# Add text labels inside or above the bars
text(bp, huellen_farben$Nennung, labels = huellen_farben$Nennung, pos = 3, cex = 0.8)
# Specify the colors corresponding to each category
farben_colors <- c("white", "black", "green", "orange", "red")
# Create the barplot using the specified colors
bp <- barplot(height = huellen_farben$Nennung,
names = huellen_farben$Farbe,
col = farben_colors,
main = "Anzahl der Nennungen nach Farbe",
xlab = "Farbe",
ylab = "Anzahl der Nennungen")
# Add text labels inside or above the bars
text(bp, huellen_farben$Nennung, labels = huellen_farben$Nennung, pos = 3, cex = 0.8)
# Specify the colors corresponding to each category
farben_colors <- c("white", "black", "green", "orange", "red")
# Create the barplot using the specified colors
bp <- barplot(height = huellen_farben$Nennung,
names = huellen_farben$Farbe,
col = farben_colors,
main = "Anzahl der Nennungen nach Farbe",
xlab = "Farbe",
ylab = "Anzahl der Nennungen")
# Add text labels inside or above the bars
text(bp, huellen_farben$Nennung, labels = huellen_farben$Nennung, pos = 3, cex = 0.8)
# Add text labels inside or above the bars
text(bp, huellen_farben$Nennung, labels = huellen_farben$Nennung, pos = 2, cex = 0.8)
farben_colors <- c("white", "black", "green", "orange", "red")
# Create the barplot using the specified colors
bp <- barplot(height = huellen_farben$Nennung,
names = huellen_farben$Farbe,
col = farben_colors,
main = "Anzahl der Nennungen nach Farbe",
xlab = "Farbe",
ylab = "Anzahl der Nennungen")
# Add text labels inside or above the bars
text(bp, huellen_farben$Nennung, labels = huellen_farben$Nennung, pos = 2, cex = 0.8)
farben_colors <- c("white", "black", "green", "orange", "red")
# Create the barplot using the specified colors
bp <- barplot(height = huellen_farben$Nennung,
names = huellen_farben$Farbe,
col = farben_colors,
main = "Anzahl der Nennungen nach Farbe",
xlab = "Farbe",
ylab = "Anzahl der Nennungen")
# Add text labels inside or above the bars
text(bp, huellen_farben$Nennung, labels = huellen_farben$Nennung, pos = 4, cex = 0.8)
farben_colors <- c("white", "black", "green", "orange", "red")
# Create the barplot using the specified colors
bp <- barplot(height = huellen_farben$Nennung,
names = huellen_farben$Farbe,
col = farben_colors,
main = "Anzahl der Nennungen nach Farbe",
xlab = "Farbe",
ylab = "Anzahl der Nennungen")
# Add text labels inside or above the bars
text(bp, huellen_farben$Nennung, labels = huellen_farben$Nennung, pos = 1, cex = 0.8)
bp <- barplot(height = huellen_farben$Nennung,
names = huellen_farben$Farbe,
col = farben_colors,
main = "Anzahl der Nennungen nach Farbe",
xlab = "Farbe",
ylab = "Anzahl der Nennungen")
# Add text labels inside or above the bars
text(bp, huellen_farben$Nennung, labels = huellen_farben$Nennung, pos = 3, cex = 0.8)
farben_colors <- c("white", "black", "green", "orange", "red")
# Create the barplot using the specified colors
bp <- barplot(height = huellen_farben$Nennung,
names = huellen_farben$Farbe,
col = farben_colors,
main = "Anzahl der Nennungen nach Farbe",
xlab = "Farbe",
ylab = "Anzahl der Nennungen")
# Add text labels inside or above the bars
text(bp, huellen_farben$Nennung, labels = huellen_farben$Nennung, pos = 3.5, cex = 0.8)
farben_colors <- c("white", "black", "green", "orange", "red")
# Create the barplot using the specified colors
bp <- barplot(height = huellen_farben$Nennung,
names = huellen_farben$Farbe,
col = farben_colors,
main = "Anzahl der Nennungen nach Farbe",
xlab = "Farbe",
ylab = "Anzahl der Nennungen")
# Add text labels inside or above the bars
text(bp, huellen_farben$Nennung, labels = huellen_farben$Nennung, pos = 3, cex = 0.8)
# Specify the colors corresponding to each category
farben_colors <- c("white", "black", "green", "orange", "red")
# Create the barplot using the specified colors
bp <- barplot(height = 500,
names = huellen_farben$Farbe,
col = farben_colors,
main = "Anzahl der Nennungen nach Farbe",
xlab = "Farbe",
ylab = "Anzahl der Nennungen")
farben_colors <- c("white", "black", "green", "orange", "red")
# Create the barplot using the specified colors
bp <- barplot(height = huellen_farben$Nennung,
names = huellen_farben$Farbe,
col = farben_colors,
main = "Anzahl der Nennungen nach Farbe",
xlab = "Farbe",
ylab = "Anzahl der Nennungen")
# Add text labels inside or above the bars
text(bp, huellen_farben$Nennung, labels = huellen_farben$Nennung, pos = 3, cex = 0.8)
q()
knitr::opts_chunk$set(echo = TRUE)
library(knitr)
Farbe <- c("hellweiß", "schwarzmetallic", "limette", "orange", "rot")
Nennung <- c(130, 104, 325, 455, 286)
huellen_farben <- data.frame(Farbe, Nennung)
kable(huellen_farben)
# Specify the colors corresponding to each category
farben_colors <- c("white", "black", "green", "orange", "red")
# Create the barplot using the specified colors
bp <- barplot(height = huellen_farben$Nennung,
names = huellen_farben$Farbe,
col = farben_colors,
ylim = c(0,500),
main = "Anzahl der Nennungen nach Farbe",
xlab = "Farbe",
ylab = "Anzahl der Nennungen")
# Add text labels inside or above the bars
text(bp, huellen_farben$Nennung, labels = huellen_farben$Nennung, pos = 3, cex = 1)
# Specify the colors corresponding to each category
farben_colors <- c("white", "black", "green", "orange", "red")
# Create the barplot using the specified colors
bp <- barplot(height = huellen_farben$Nennung,
names = huellen_farben$Farbe,
col = farben_colors,
ylim = c(0,500),
main = "Anzahl der Nennungen nach Farbe",
xlab = "Farbe",
ylab = "Anzahl der Nennungen")
# Add text labels inside or above the bars
text(bp, huellen_farben$Nennung, labels = huellen_farben$Nennung, pos = 3, cex = 0,5)
# Specify the colors corresponding to each category
farben_colors <- c("white", "black", "green", "orange", "red")
# Create the barplot using the specified colors
bp <- barplot(height = huellen_farben$Nennung,
names = huellen_farben$Farbe,
col = farben_colors,
ylim = c(0,500),
main = "Anzahl der Nennungen nach Farbe",
xlab = "Farbe",
ylab = "Anzahl der Nennungen")
# Add text labels inside or above the bars
text(bp, huellen_farben$Nennung, labels = huellen_farben$Nennung, pos = 1, cex = 0,5)
# Specify the colors corresponding to each category
farben_colors <- c("white", "black", "green", "orange", "red")
# Create the barplot using the specified colors
bp <- barplot(height = huellen_farben$Nennung,
names = huellen_farben$Farbe,
col = farben_colors,
ylim = c(0,500),
main = "Anzahl der Nennungen nach Farbe",
xlab = "Farbe",
ylab = "Anzahl der Nennungen")
# Add text labels inside or above the bars
text(bp, huellen_farben$Nennung, labels = huellen_farben$Nennung, pos = 2, cex = 0,5)
# Specify the colors corresponding to each category
farben_colors <- c("white", "black", "green", "orange", "red")
# Create the barplot using the specified colors
bp <- barplot(height = huellen_farben$Nennung,
names = huellen_farben$Farbe,
col = farben_colors,
ylim = c(0,500),
main = "Anzahl der Nennungen nach Farbe",
xlab = "Farbe",
ylab = "Anzahl der Nennungen")
# Add text labels inside or above the bars
text(bp, huellen_farben$Nennung, labels = huellen_farben$Nennung, pos = 3, cex = 0,5)
# Specify the colors corresponding to each category
farben_colors <- c("white", "black", "green", "orange", "red")
# Create the barplot using the specified colors
bp <- barplot(height = huellen_farben$Nennung,
names = huellen_farben$Farbe,
col = farben_colors,
ylim = c(0,500),
main = "Anzahl der Nennungen nach Farbe",
xlab = "Farbe",
ylab = "Anzahl der Nennungen")
# Add text labels inside or above the bars
text(bp, huellen_farben$Nennung, pos = 3, cex = 0,5)
# Specify the colors corresponding to each category
farben_colors <- c("white", "black", "green", "orange", "red")
# Create the barplot using the specified colors
bp <- barplot(height = huellen_farben$Nennung,
names = huellen_farben$Farbe,
col = farben_colors,
ylim = c(0,500),
main = "Anzahl der Nennungen nach Farbe",
xlab = "Farbe",
ylab = "Anzahl der Nennungen")
# Add text labels inside or above the bars
text(bp, labels = huellen_farben$Nennung, pos = 3, cex = 0,5)
# Specify the colors corresponding to each category
farben_colors <- c("white", "black", "green", "orange", "red")
# Create the barplot using the specified colors
bp <- barplot(height = huellen_farben$Nennung,
names = huellen_farben$Farbe,
col = farben_colors,
ylim = c(0,500),
main = "Anzahl der Nennungen nach Farbe",
xlab = "Farbe",
ylab = "Anzahl der Nennungen")
# Add text labels inside or above the bars
text(bp, huellen_farben$Nennung, labels = huellen_farben$Nennung, pos = 3, cex = 0,5)
library(knitr)
summe_nennungen <- sum(Nennung)
prozentuale_anteile <- Nennung / summe_nennungen
absolute_anzahl <- 1000000 * prozentuale_anteile
ergebnisse <- data.frame(Hüllen_Farbe=Farbe, Herstellungs_anzahl=absolute_anzahl)
kable(ergebnisse)
library(knitr)
setwd("C:/Users/rafaa/Desktop/HTW MODULE/Semester 3/Statistik/data")
# Erstelle das Histogramm mit den spezifizierten Gruppen
hist(data$Profit,
main = "Frequenz von Profit",
xlab = "Profit",
ylab = "Frequenz",
breaks = c(seq(0,3000, 250), 3500))
bp2 <- barplot(freq_table,
main = "Häufigkeit der Ausprägungen von Previous",
xlab = "Previous",
ylab = "Häufigkeit",
ylim = c(0,85),
col = rainbow(nrow(freq_table)))
setwd("C:/Users/PC/Desktop/HTW-Notes/Semester 3/Statistik/data")
data <- read.csv2("Applewood_2011.csv")
getwd()
setwd("C:/Users/PC/Desktop/HTW-Notes/Semester 3/Statistik/data")
getwd()
setwd("C:/Users/PC/Desktop/HTW-Notes/Semester 3/Statistik/data")
getwd()
data <- read.csv2("Applewood_2011.csv")
library(knitr)
setwd("C:/Users/PC/Desktop/HTW-Notes/Semester 3/Statistik/data")
getwd()
data <- read.csv2("Applewood_2011.csv")
# Ausprägungen des Merkmals *Previous*
previous_ausprä <- data$Previous
# absolute Häufigkeit
freq_table <- table(previous_ausprä)
freq_table
# relative Häufigkeit
relative_freq <- prop.table(freq_table)
relative_freq
# kumulierte absolute Häufigkeit
cumulative_freq <- cumsum(freq_table)
cumulative_freq
# kumulierte relative Häufigkeit
cumulative_relative_freq <- cumsum(relative_freq)
cumulative_relative_freq
# *Data Frame* erstelle
freq_df <- data.frame(
Previous = as.numeric(names(freq_table)),
Absolute_Frequency = as.numeric(freq_table),
Relative_Frequency = round(relative_freq, 3),
Cumulative_Frequency = cumulative_freq,
Cumulative_Relative_Frequency = round(cumulative_relative_freq, 3)
)
kable(freq_df)
bp2 <- barplot(freq_table,
main = "Häufigkeit der Ausprägungen von Previous",
xlab = "Previous",
ylab = "Häufigkeit",
ylim = c(0,85),
col = rainbow(nrow(freq_table)))
text(bp2, freq_table, labels = freq_table, pos = 3, cex = 1)
# Erstelle das Histogramm mit den spezifizierten Gruppen
hist(data$Profit,
main = "Frequenz von Profit",
xlab = "Profit",
ylab = "Frequenz",
breaks = c(seq(0,3000, 250), 3500))
# Berechne die relative kumulative Häufigkeitsverteilung von Profit
ecdf_profit <- ecdf(data$Profit)
# Plotte die relative kumulative Häufigkeitsverteilung
plot(ecdf_profit, main = "Relative kumulierte Häufigkeitsverteilung von Profit",
xlab = "Profit", ylab = "Kumulierte Häufigkeit")
# Erstelle das Histogramm mit den spezifizierten Gruppen
hist(data$Profit,
main = "Frequenz von Profit",
xlab = "Profit",
ylab = "Frequenz",
breaks = c(seq(0,3000, 250), 5500))
# Berechne die relative kumulative Häufigkeitsverteilung von Profit
ecdf_profit <- ecdf(data$Profit)
# Plotte die relative kumulative Häufigkeitsverteilung
plot(ecdf_profit, main = "Relative kumulierte Häufigkeitsverteilung von Profit",
xlab = "Profit", ylab = "Kumulierte Häufigkeit")
# Erstelle das Histogramm mit den spezifizierten Gruppen
hist(data$Profit,
main = "Frequenz von Profit",
xlab = "Profit",
ylab = "Frequenz",
breaks = c(seq(0,3000, 250), 3500))
# Berechne die relative kumulative Häufigkeitsverteilung von Profit
ecdf_profit <- ecdf(data$Profit)
# Plotte die relative kumulative Häufigkeitsverteilung
plot(ecdf_profit, main = "Relative kumulierte Häufigkeitsverteilung von Profit",
xlab = "Profit", ylab = "Kumulierte Häufigkeit")
?text
# Specify the colors corresponding to each category
farben_colors <- c("white", "black", "green", "orange", "red")
# Create the barplot using the specified colors
bp <- barplot(height = huellen_farben$Nennung,
names = huellen_farben$Farbe,
col = farben_colors,
ylim = c(0,500),
main = "Anzahl der Nennungen nach Farbe",
xlab = "Farbe",
ylab = "Anzahl der Nennungen")
# Add text labels inside or above the bars
#text(bp, huellen_farben$Nennung, labels = huellen_farben, pos = 3, cex = 0,5)
# Specify the colors corresponding to each category
farben_colors <- c("white", "black", "green", "orange", "red")
# Create the barplot using the specified colors
bp <- barplot(height = huellen_farben$Nennung,
names = huellen_farben$Farbe,
col = farben_colors,
ylim = c(0,500),
main = "Anzahl der Nennungen nach Farbe",
xlab = "Farbe",
ylab = "Anzahl der Nennungen")
# Add text labels inside or above the bars
#text(bp, huellen_farben$Nennung, labels = huellen_farben, pos = 3, cex = 0,5)
text(Nennung, labels = Nennung)
# Specify the colors corresponding to each category
farben_colors <- c("white", "black", "green", "orange", "red")
# Create the barplot using the specified colors
bp <- barplot(height = huellen_farben$Nennung,
names = huellen_farben$Farbe,
col = farben_colors,
ylim = c(0,500),
main = "Anzahl der Nennungen nach Farbe",
xlab = "Farbe",
ylab = "Anzahl der Nennungen")
# Add text labels inside or above the bars
#text(bp, huellen_farben$Nennung, labels = huellen_farben, pos = 3, cex = 0,5)
text(Nennung, labels = Nennung, pos = 3)
# Specify the colors corresponding to each category
farben_colors <- c("white", "black", "green", "orange", "red")
# Create the barplot using the specified colors
bp <- barplot(height = huellen_farben$Nennung,
names = huellen_farben$Farbe,
col = farben_colors,
ylim = c(0,500),
main = "Anzahl der Nennungen nach Farbe",
xlab = "Farbe",
ylab = "Anzahl der Nennungen")
# Add text labels inside or above the bars
text(huellen_farben$Nennung, labels = huellen_farben, pos = 3, cex = 0,5)
# Specify the colors corresponding to each category
farben_colors <- c("white", "black", "green", "orange", "red")
# Create the barplot using the specified colors
bp <- barplot(height = huellen_farben$Nennung,
names = huellen_farben$Farbe,
col = farben_colors,
ylim = c(0,500),
main = "Anzahl der Nennungen nach Farbe",
xlab = "Farbe",
ylab = "Anzahl der Nennungen")
# Add text labels inside or above the bars
text(bp, huellen_farben$Nennung, labels = huellen_farben, pos = 3, cex = 0,5)
# Specify the colors corresponding to each category
farben_colors <- c("white", "black", "green", "orange", "red")
# Create the barplot using the specified colors
bp <- barplot(height = huellen_farben$Nennung,
names = huellen_farben$Farbe,
col = farben_colors,
ylim = c(0,500),
main = "Anzahl der Nennungen nach Farbe",
xlab = "Farbe",
ylab = "Anzahl der Nennungen")
# Add text labels inside or above the bars
text(bp, huellen_farben$Nennung, labels = huellen_farben$Nennung, pos = 3, cex = 0,5)
# Specify the colors corresponding to each category
farben_colors <- c("white", "black", "green", "orange", "red")
# Create the barplot using the specified colors
bp <- barplot(height = huellen_farben$Nennung,
names = huellen_farben$Farbe,
col = farben_colors,
ylim = c(0,500),
main = "Anzahl der Nennungen nach Farbe",
xlab = "Farbe",
ylab = "Anzahl der Nennungen")
# Add text labels inside or above the bars
text(Nennung, huellen_farben$Nennung, labels = huellen_farben$Nennung, pos = 3, cex = 0,5)
# Specify the colors corresponding to each category
farben_colors <- c("white", "black", "green", "orange", "red")
# Create the barplot using the specified colors
bp <- barplot(height = huellen_farben$Nennung,
names = huellen_farben$Farbe,
col = farben_colors,
ylim = c(0,500),
main = "Anzahl der Nennungen nach Farbe",
xlab = "Farbe",
ylab = "Anzahl der Nennungen")
# Add text labels inside or above the bars
text(bp, huellen_farben$Nennung, labels = huellen_farben$Nennung, pos = 3, cex = 0,5)
library(knitr)
summe_nennungen <- sum(Nennung)
prozentuale_anteile <- Nennung / summe_nennungen
absolute_anzahl <- 1000000 * prozentuale_anteile
ergebnisse <- data.frame(Hüllen_Farbe=Farbe, Herstellungs_anzahl=absolute_anzahl)
kable(ergebnisse)
f <- prop.table(Nennung)
f * 1000000
